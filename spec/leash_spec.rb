# frozen_string_literal: true

RSpec.describe Leash do
  it "has a version number" do
    expect(Leash::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
